/**
 * Simplificação de funções matemáticas muito usadas 
 */
static class Math {
  
  Math() {}
  
  static float log10(float x) {
    return (log(x) / log(10));
  }
  
  static float log2(float x) {
    return (log(x) / log(2));
  }
  
}
